<head>
    <link rel="stylesheet" href="/css/leaflet.css" >
    <link rel="stylesheet" href="/css/go-leaflet.css" >
    <script src="/js/leaflet.js"></script>
</head>
<body>
    <h1>{{.title}}</h1>
    <div id="mapid"></div>
</body>    
<script type="text/javascript" >
var map = L.map('mapid', {
crs: L.CRS.Simple,
minZoom: -2
});
var bounds = [[0,0], [1800,2880]];
var image = L.imageOverlay('/images/testmap.png', bounds).addTo(map);
map.fitBounds(bounds);
{{ call .markers }}
</script>